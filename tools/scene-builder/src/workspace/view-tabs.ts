/**
 * View tabs — no-op.
 *
 * Replaced by pipeline-layout.ts. Kept as a no-op export so existing
 * imports in workspace.ts don't break.
 */

/** No-op — pipeline layout replaces zone tabs. */
export function initViewTabs(): void {
  // No-op: pipeline-layout.ts now handles zone switching
}
