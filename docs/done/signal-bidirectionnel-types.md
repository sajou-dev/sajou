# Signaux bidirectionnels — types user.*
Tiers: core
---
5 types de signaux utilisateur dans @sajou/schema pour le canal retour :
user.click, user.move, user.zone, user.command, user.point.

## Livré
- 5 types user.* dans @sajou/schema (TypeScript + JSON Schema)
- Payloads typés (UserClickPayload, UserMovePayload, etc.)
- Tests de type safety + narrowing (6 tests)

Mergé dans v0.1.0.
